import { BarChart3 } from "lucide-react";
import { ComingSoon } from "@/components/coming-soon";

export default function ReportsPage() {
  return (
    <ComingSoon
      icon={BarChart3}
      title="Reports"
      description="Deeper pipeline and rep-performance reporting lands in a later phase — the Dashboard already has a pipeline-value chart to start from."
    />
  );
}
